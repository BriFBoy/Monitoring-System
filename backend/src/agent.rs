use std::net::UdpSocket;

use crate::{IpAddr, SystemMectrics};

/*
pub fn get_agent_metrics(
    ipaddr: IpAddr,
    udp_sock: &UdpSocket,
) -> Result<SystemMectrics, std::io::Error> {
    Ok(SystemMectrics::from_agent_response(&response))
}*/
